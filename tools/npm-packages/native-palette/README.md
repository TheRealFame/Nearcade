# @nearcade/native-palette

Cross-platform deep semantic OS UI theme extraction (KDE, GTK, Windows, macOS).

## Usage

```js
const palette = require('@nearcade/native-palette');

const theme = palette.getTheme();
// Returns theme information depending on the OS
```

This package uses artificial intelligence large language models for code generation and structure planning.
